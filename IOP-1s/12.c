#include <stdio.h>

int main() {
    // Declare variable
    char ch;

    // Input character
    printf("Enter a character: ");
    scanf("%c", &ch);

    // Print ASCII code
    printf("ASCII code: %d\n", ch);

    return 0;
}
